@using System;
@using Sandbox;
@using Sandbox.UI;

@namespace Jigsaw
@inherits RootPanel

<style>
	RootHud {
/*		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;*/
	}

	.debugText {
		font-size: 15px;
		color: white;
		font-family: Poppins;
		font-weight: 600;
		text-align: left;

		width:100%;
		height: 100%;
		margin-left:25px;
		margin-top:25px;


		text-stroke: 3px black;
	}

</style>

@code {
	public static RootHud Current;

	public RootHud()
	{
		Current = this;

		AddChild<Jigsaw.Crosshair>();
		AddChild<Jigsaw.ChatBox>();
		AddChild<VoiceList>();
		AddChild<VoiceSpeaker>();
		AddChild<VotingTimer>();
		AddChild<Actionfeed>();
		AddChild<LeaderInfo>();

	}

	protected override int BuildHash()
	{
		return Time.Now.GetHashCode();
	}
}
