@using System;
@using Sandbox;
@using Sandbox.UI;

@namespace Jigsaw
@inherits RootPanel

<style>
	RootHud {
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
	}

	.debugText {
		font-size: 25px;
		color: white;
		font-family: Poppins;
		font-weight: 600;
		text-align: left;

		width:100%;
		height: 100%;
		margin-left:50px;
		margin-top:25px;


		text-stroke: 5px black;
	}

</style>

<root>

	<panel class="debugText">
Enter an Image URL into chat to play!
[F] Toggle debug display
	</panel>

</root>

@code {
	public static RootHud Current;

	public RootHud()
	{
		Current = this;

		//AddChild<PickupFeed>();
		AddChild<Jigsaw.Crosshair>();
		AddChild<Jigsaw.ChatBox>();
		//AddChild<ChatBox>();
		//Scoreboard = AddChild<Scoreboard>();
		AddChild<VoiceList>();
		//AddChild<VoiceSpeaker>();
		AddChild<VotingTimer>();
	}

	protected override int BuildHash()
	{
		return Time.Now.GetHashCode();
	}
}
